package seeders

import (
	"fmt"
)

// SeedBusiness seeds business profiles and members
func SeedBusiness(ctx *SeedContext) error {
	fmt.Println("    Business seeding skipped (implement as needed)")
	return nil
}

// SeedBooking seeds bookings and calendar events
func SeedBooking(ctx *SeedContext) error {
	fmt.Println("    Booking seeding skipped (implement as needed)")
	return nil
}

// SeedReview seeds reviews
func SeedReview(ctx *SeedContext) error {
	fmt.Println("    Review seeding skipped (implement as needed)")
	return nil
}

// SeedFinance seeds wallets and transactions
func SeedFinance(ctx *SeedContext) error {
	fmt.Println("    Finance seeding skipped (implement as needed)")
	return nil
}
