# Queue Configuration (Cloud Tasks)
# This file defines queue settings for job processing

queue:
  # Queue mappings (key=purpose, value=Cloud Tasks queue name)
  subjects:
    email: "email-queue"
    notification: "notification-queue"
    moderation: "moderation-queue"
    media_thumbnail: "media-thumbnail-queue"
    media_cleanup: "media-cleanup-queue"
    ai_moderation: "ai-moderation-queue"
    booking_expiry: "booking-expiry-queue"
    booking_completion: "booking-completion-queue"
    booking_checkin_out: "booking-checkin-out-queue"
    booking_refund: "booking-refund-queue"
    payment_webhook: "payment-webhook-queue"
    payout_process: "payout-process-queue"
    payout_retry: "payout-retry-queue"
    finance_reconciliation: "finance-reconciliation-queue"
    review_stats: "review-stats-queue"
    review_standoff: "review-standoff-queue"
    review_reminders: "review-reminders-queue"
    promotion_expiry: "promotion-expiry-queue"
    subscription_billing: "subscription-billing-queue"
    calendar_showing_reminders: "calendar-showing-reminders-queue"
    calendar_open_house_reminders: "calendar-open-house-reminders-queue"
    verification_submission: "verification-submission-queue"
    verification_sms: "verification-sms-queue"
    verification_reconciliation: "verification-reconciliation-queue"
    interactions_batch: "interactions-batch-queue"
    interactions_aggregator: "interactions-aggregator-queue"
    conversation_cleanup: "conversation-cleanup-queue"


